- name: Delete inventory entry
  ansible.builtin.blockinfile:
    marker_begin: "{{ host_marker }} BEGIN"
    marker_end: "{{ host_marker }} END"
    path: "{{ playbook_dir }}/hosts"
    state: absent